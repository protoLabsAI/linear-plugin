[pytest]
# In tests/ so rootdir is here, not the plugin package root — pytest never imports
# the plugin's __init__.py (relative imports resolve only at runtime as the `linear`
# package). importlib mode + file-path module loading in the test.
addopts = --import-mode=importlib
